import React from 'react';
import '../assets/styles/components/ContainerDashboard.scss';

const ContainerDashboard = ({ children }) => (
	<section className='container'>{children}</section>
);

export default ContainerDashboard;
